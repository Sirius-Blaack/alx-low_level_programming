#include <stdio.h>
/**
 * main - A program that prints a line with with puts functions
 * Return: 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle0);
return (0);
}
